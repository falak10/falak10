name: Update Activity

permissions:
  contents: write

on:
  # STOPPED THE BOT HERE:
  # schedule:
  #   - cron: "*/30 * * * *"
  
  # Keep this if you want to run it manually later, or comment it out too:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    name: Update this repo's README with recent activity
    # ... (rest of the file stays the same)
